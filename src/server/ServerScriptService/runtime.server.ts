import { ServerStorage } from "@rbxts/services";
import initialize from "shared/Modules/Utils/Initialize";

// folders
const Modules = ServerStorage.FindFirstChild("Modules") as Folder;

throw"create: Meteorite(BasePart)/FireSword(BasePart) in ReplicatedStorage -> Assets -> Weapons"

// setup
initialize(Modules);